<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile User</title>
    <link rel="stylesheet" href="/styles/output.css" />
  </head>

  <body class="bg-[#fafafa] min-h-screen flex justify-center items-center">
    <div class="w-full max-w-4xl p-6 bg-white shadow-md rounded-xl">
      <!-- Judul Halaman -->
      <h1 class="pb-2 mb-4 text-xl font-semibold text-gray-800 border-b">
        Profile User
      </h1>

      <!-- Kartu Profil -->
      <div
        class="relative overflow-hidden bg-white border border-gray-200 rounded-lg shadow-sm"
      >
        <div class="absolute top-0 left-0 w-full h-1 bg-orange-400"></div>

        <div
          class="flex flex-col items-start justify-between gap-6 p-6 md:flex-row"
        >
          <div class="flex-1">
            <h2 class="text-2xl font-semibold text-gray-800">
              <%= user.name.first %> <%= user.name.last %>
            </h2>
            <p class="mb-3 text-sm text-gray-500"><%= user.email %></p>

            <!-- Gender -->
            <div class="mb-5">
              <p class="mb-1 text-sm font-semibold text-gray-700">Gender</p>
              <span
                class="px-3 py-1 text-xs font-medium text-orange-500 border border-orange-400 rounded-full"
              >
                <%= user.gender %>
              </span>
            </div>

            <!-- Grid Info -->
            <div class="grid grid-cols-2 text-sm gap-y-3">
              <div>
                <p class="text-xs font-semibold text-gray-400">Username</p>
                <p class="font-medium text-gray-800">
                  <%= user.login.username %>
                </p>
              </div>
              <div>
                <p class="text-xs font-semibold text-gray-400">Telepon</p>
                <p class="font-medium text-gray-800"><%= user.phone %></p>
              </div>
              <div>
                <p class="text-xs font-semibold text-gray-400">Negara</p>
                <p class="font-medium text-gray-800">
                  <%= user.location.country %>
                </p>
              </div>
              <div>
                <p class="text-xs font-semibold text-gray-400">Kota</p>
                <p class="font-medium text-gray-800">
                  <%= user.location.city %>
                </p>
              </div>
              <div>
                <p class="text-xs font-semibold text-gray-400">Usia</p>
                <p class="font-medium text-gray-800">
                  <%= user.dob.age %> tahun
                </p>
              </div>
              <div>
                <p class="text-xs font-semibold text-gray-400">Kode Pos</p>
                <p class="font-medium text-gray-800">
                  <%= user.location.postcode %>
                </p>
              </div>
            </div>
          </div>

          <!-- Foto Profil -->
          <div class="flex justify-center w-full md:w-32 md:justify-end">
            <img
              src="<%= user.picture.large %>"
              alt="Profile Picture"
              class="object-cover border border-gray-300 rounded-lg shadow-sm w-28 h-28"
            />
          </div>
        </div>
      </div>

      <!-- Tombol Refresh -->
      <div class="flex justify-end mt-4">
        <button
          onclick="location.reload()"
          class="px-4 py-2 text-sm font-medium text-white transition bg-orange-500 rounded-md hover:bg-orange-600"
        >
          Refresh User
        </button>
      </div>
    </div>
  </body>
</html>
